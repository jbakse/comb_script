Ellipse:
    keyword: ellipse
    class: Ellipse
    extends: Region

    description: |
        <p>
        Ellipse draws an ellipse.
        <p>
        <pre>
        children:
            - ellipse:
                properties:
                    width: 200
                    height: 200
        </pre>

    properties:
        # do we want diameter or radius in here?
