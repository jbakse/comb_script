Ellipse:
    keyword: ellipse
    class: Ellipse
    extends: Region

    description: |
        <p>
        Ellipse draws an ellipse.
        <p>
        <pre>
        children:
            - ellipse:
                properties:
                    width: 200px
                    height: 200px
        </pre>

    properties:
        # do we want diameter or radius in here?
