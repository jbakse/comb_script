properties:
    width: 250mm
    height: 150mm

children:
    - polygon:
        properties:
            radius: 50mm
            #when radius is set width and height are only used for adjusting the container
            #scale is ignored all together
            width: 125mm
            margin_left: 0mm
    - polygon:
        properties:
            width: 125mm
            margin_right: 0mm
            scale: fit