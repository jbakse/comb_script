#Always use units when declaring constants

properties:
    name: variables
    unit: mm
    width: 100 
    height: 100 

library:
    #assign constants
    - &size 75 mm
    - &size2 25 mm

children:
    - rectangle:
        properties:
            #use constants
            width: *size
            height: *size2