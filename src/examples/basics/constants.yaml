# You can declare constants on the document (or a region) and
# use them in expressions in child regions.
# see: constants

properties:
    width: 3in
    height: 3in

constants:
    size: 1in
    golden_ratio: 1.618

children:
    - rectangle:
        properties:
            width: size
            height: size * golden_ratio
