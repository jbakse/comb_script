# http://mathjs.org/
properties:
    name: bounds
    unit: mm
    width: 100
    height: 50
    zoom: .25

children:
    - ellipse:
        properties:

            width: parent_height
            height: parent_height * .5
            left: parent_right
            # height: 2cm * 1.5
