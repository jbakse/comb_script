# http://mathjs.org/
properties:
    name: bounds
    
    width: 100
    height: 1 in
    # zoom: .25

children:
    - ellipse:
        properties:
            name: number_one
            width: parent_height
            height: parent_height * .
            left: parent_right
            # height: 2cm * 1.5
    - ellipse:
        properties:
            name: number_two
            width: parent_height
            height: parent_height * .
            left: parent_right
            # height: 2cm * 1.5
