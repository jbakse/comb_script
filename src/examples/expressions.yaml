# http://mathjs.org/
properties:
    name: bounds
    width: 1 in
    height: 1 in
    # zoom: .25
constants:
    table_height: 2in


children:
    - rectangle:
        properties:
            radius: .125 in
            top: parent_top + 10mm
